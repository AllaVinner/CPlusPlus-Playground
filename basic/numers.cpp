#include<iostream>
#include<cmath>
using namespace std;

int main()
{
    // % modulo
    int mod = 15 % 4;
    double  dnum = 5.5;
    double a = dnum ++;
    // math with a decimal and a integer
    // the integer will be cast to decimal
    // math with integer will allways return 
    // integers though. 
    
    //power 
    int p = pow(3,5);
    cout << p;
}